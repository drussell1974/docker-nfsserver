version: "3"
services:
        nfs_server:
                image: drussell1974/nfs-server:v0.0.5-alpha
                external_links:
                        - ldap_server
                ports:
                        - 2049:2049
                environment:
                        - NFS_IPADDR_ALLOWED=${NFS_IPADDR_ALLOWED}
                        - NFS_PUBLIC_DIR=${NFS_PUBLIC_PATH}
                        - NFS_HOME_PATH=${NFS_HOME_PATH}
                          #volumes:
                          #- ${NFS_HOST_PUBLIC_PATH}:${NFS_PUBLIC_PATH}
                          #- ${NFS_HOST_HOME_PATH}:${NFS_HOME_PATH}
