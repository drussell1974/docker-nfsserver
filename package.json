{
  "name": "docker-openldap",
  "version": "1.0.0",
  "description": "an nfs server with docker",
  "main": "index.js",
  "author": "Dave Russell",
  "license": "MIT",
  "repository": "https://github.com/drussell1974/docker-nfsserver",
  "scripts" : {
	  "build":"sed '/build:/d; /# TODO:/d;' docker-compose.yml > docker-compose.yml.example   &&  tar --exclude-from=.gitignore -czvf  nfs-server.tar.gz docker-compose.yml.example .env.example build/Dockerfile build/docker-entrypoint.sh"
  }
}
